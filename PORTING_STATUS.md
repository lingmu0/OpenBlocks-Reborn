# OpenBlocks Reborn 移植状态

本项目将 OpenBlocks 1.8.1（Minecraft 1.12.2 / Forge / OpenModsLib）重写到 Minecraft 1.21.1、NeoForge 21.1.233 和 Java 21。模组 ID 为 `openblocks_reborn`，当前版本为 `1.0.0-1.21.1`。

## 状态和验收规则

- **✅ 已验收**：不仅完成代码，还通过了对应的 GameTest、实际客户端烟雾测试或明确的人工游戏反馈。
- **🟡 待复验**：代码、资源和编译已经完成，但仍缺少该功能的针对性游戏内交互验证。
- **⬜ 未实现**：尚未进入当前 NeoForge 实现。
- `classes` 或 JSON 解析通过只能证明代码及资源结构有效，不能代替方块交互和视觉验收。
- 每次继续移植时必须同步本文件；只有补充了对应的验证依据，项目才会把 🟡 改为 ✅。

## 已实现内容

- 当前注册 40 个方块和 43 个物品入口，并把旧 metadata 物品拆成稳定的现代注册 ID。新增小油漆刷和小刮刀作为单像素精细工具。OpenBlocks 脚手架已删除，使用 Minecraft 原版脚手架。
- 导入并转换旧版方块、物品、声音与动画资源；所有本地模型和纹理引用均能解析。
- 恢复特殊模型和状态模型：大炮、捕兽夹、旗帜、喷洒器、风扇、投影仪、经验排水口/花洒、储罐、战利品雕像、建筑向导、虚构方块等。
- 有朝向的方块包含放置朝向、旋转/镜像状态及模型旋转。方块放置器、物品投掷器和方块破坏器支持六个输出方向；方块放置器会把输出方向传给熔炉、楼梯、原木等方块，方块破坏器会把掉落物输出到背面的容器。方块放置器恢复独立背面贴图，四个侧面的纹路会随正面方向连续旋转。
- 旧版 15 类 GUI 入口均已改为 NeoForge 自定义菜单：自动铁砧、自动附魔台、大按钮、方块放置器、物品投掷器、物品大炮、`/dev/null`、捐赠站、绘图台、行李箱、调漆机、投影仪、喷洒器、真空漏斗和经验装瓶机。界面使用 Minecraft 原版容器贴图、槽位、按钮、复选框和滑块精灵；所有自绘主面板及独立分面配置面板的四角使用原版容器相同的三像素切角圆角与黑/白/灰边框。普通文字统一使用原版容器的 `0x404040` 颜色且无阴影，按钮保留原版白字阴影。按钮悬停使用原版 highlighted 状态，自动附魔等级、投掷速度、色相/亮度和饱和度滑条均可按住拖动。物品投掷器和物品大炮使用连续 3×3 九宫格，并保留速度、红石与大炮目标信息侧栏；九宫格及信息区不再额外套凹槽外框。经验槽和喷洒器水量槽均支持悬停精确数值提示，水量标签位于液量柱上方且不覆盖物品槽。
- 自动铁砧、自动附魔台和经验装瓶机恢复旧版分通道六面配置；每个输入、输出或经验液通道均可单独启用面，并可打开“自动”让机器主动从这些面拉取物品/经验液或推送产物。配置会同步、保存并同时约束 NeoForge 能力访问。
- 自定义液态经验、经验桶、经验排水口、装瓶机、花洒、真空漏斗和机器经验罐；支持 NeoForge 流体能力。经验排水口与花洒各自使用 1 B（1000 mB）内部缓冲。排水口会吸取上方经验球，也会按玩家实际等级和经验进度直接抽取站在上方玩家的经验，不再依赖可能失真的 `totalExperience`；花洒从背面相邻储液容器拉取经验液，并生成不会倒飞追踪玩家的下落经验球；装瓶机恢复旧版 40 tick 加工进度。经验桶已加入 `c:buckets`、`c:buckets/experience` 和 `c:experience` 流体标签；普通右键优先向可接收经验液的方块灌入，潜行右键才保留机器菜单交互，与原版水桶的使用优先级一致。
- 16 桶容量透明储罐、流体保留掉落、相邻储罐均衡网络、桶交互和动态液面渲染；相邻储罐会隐藏内部边框，同类流体液面铺满水平边界。
- 自动铁砧采用 1.21.1 原版铁砧规则，支持修复材料、同类工具合并、附魔书/附魔合并、冲突与等级上限、Prior Work Penalty、40 级限制和重命名，并按原版等级曲线消耗液态经验。自动附魔台只保留一个可拖动的等级滑条，不再提供档位按钮；原版基线上限通过 `EnchantmentHelper` 读取，运行时再经过 NeoForge 附魔等级事件读取模组调整后的上限，经验罐随上限扩容。它仍采用原版书架有效性、附魔种子、附魔池、普通书转换和青金石规则；喷洒器消耗水并支持骨粉加速。喷洒器方块自身返回不可放置、不可流动、不可见的虚拟水体 `FluidState`：其 FluidType 复用原版水并加入水标签，因此船、水敏感生物、湿润/灭火、农田等逻辑仍会把它当作水，但不会生成真实水方块。
- 方块破坏器、放置器、投掷器与物品大炮的红石脉冲行为；指示器可绑定大炮、指定目标并计算抛物线弹道。
- 捕兽夹开合模型已下移到方块底面，不再悬空，并保留红石禁用、伤害与固定生物；可染色地面/墙面旗帜及支撑检测。
- 画布每面保存 16×16 像素数据；主油漆刷普通右键会涂满点击的单面、潜行右键处理全部面，主刮刀对应擦除单面/全部面；新增小油漆刷和小刮刀用于单像素涂色/擦除。上下表面的准心命中映射已按 `CanvasRenderer` 的实际行方向纠正，不再镜像；问题来自工具坐标换算而不是画布面的模型朝向。旧版 15 种模板均已恢复真实镂空遮罩：模板可贴到单个面、透孔上色、旋转、取回，并随画布方块物品保存；模板物品和贴面覆盖层均按实际孔洞渲染，小油漆刷同样遵守模板孔洞。方块物品数据保留及像素渲染继续支持，玻璃画布同样支持。
- 绘图台恢复模板/字形两种模式、15 种模板的 16×16 实时镂空预览与文字打印；已加工模板可以重新放入并加工为其他图案，翻页按钮改为左右排列的 `<`、`>`，并下移至与输入/输出槽相同的纵向起点。调漆机恢复 CMYK 染料槽、任意 RGB 选色与 300 tick 调制进度。投影仪具有旧版半高碰撞体、地图槽、空地图升级、高度图有效状态和四向旋转 GUI；客户端直接读取 9×9 高度数据并绘制可旋转的立体地形投影，不再用无关粒子代替。
- 墓碑、治疗方块、目标、风扇、海绵、道路、电梯/旋转电梯、村庄高亮器、天空方块、绳梯和建筑向导行为；道路恢复 1.6 像素厚、无碰撞、透天光及失去下方支撑时破坏并掉落自身的旧版规则。天空方块恢复延迟一 tick 的红石状态、反转模式、不可见但保持完整碰撞的激活状态和天空颜色渲染；普通/反相状态会保留在物品中，并恢复用红石火把来回转换的旧版配方。目标的命中红石强度与红石驱动的立起/收起状态已分离；旋转电梯使用带 yaw 的服务端传送包，抵达后按目标电梯朝向可靠旋转玩家。
- 行李箱实体跟随、拾取、27 格持久化库存和物品化回收；行李箱现为具有地面导航、台阶处理和逐 tick 网络更新的寻路生物，正常跟随不再用坐标跳跃模拟移动，仅在超过 64 格的失联恢复场景传送。行李箱使用固定三维物品实体渲染而不是始终朝向镜头的平面贴图，世界渲染整体上移以露出底部脚部。行李箱拒绝所有伤害；即使被命令移除或由意外逻辑杀死，也会在原地生成无敌、无限寿命且保留名称和 27 格库存的行李箱物品掉落，并防止其他行李箱把该掉落吞入库存。金蛋孵化 Mini Me，Mini Me 使用玩家皮肤渲染。
- 战利品雕像保存实体类型、渲染生物、保留掉落数据，并有受抢夺影响的稀有掉落。
- 字符牌可选择字符、按点击位置贴墙、保存字符与朝向、渲染字体并在破坏时返还对应字符物品。
- 悬挂式滑翔翼支持右键展开/收起、跟随玩家身体朝向的三维实体渲染、空中减速和耐久消耗；渲染位置和 yaw 都锚定到玩家同一帧的插值身体姿态，避免滑翔翼实体与玩家分别接收网络插值时产生相对抽搐。睡袋支持在当前位置临时睡眠且不改变重生点。
- 眼镜和起重机背包改为原生头盔/胸甲装备，不再靠右键数据开关模拟启用；仅实际穿戴时生效。起重机背包外显模型按旧版 `ModelCraneBackpack` 恢复 8×12×8 背包、24 像素支架和 42 像素随头部转动的吊臂；独立吊臂部件会显式提交给盔甲渲染，不再因不属于原版六个身体部件而消失。吊索从旧版吊臂末端坐标连接磁铁，磁铁按旧版 6/4/2 像素三层模型及 UV 渲染。被吸住的方块使用 1 tick 同步间隔的专用实体、服务端逐 tick 限速追随和客户端插值，不再沿用原版 20 tick 更新的下落方块而产生跳动。控制器与旧版默认配置一致：普通按住右键回收、潜行按住右键下放、挥动切换磁铁，并恢复升降 3 状态 × 磁铁 3 状态的组合贴图。
- 黄金之眼潜行右键打开原版风格结构绑定界面，列出当前数据包注册的结构 ID，支持文本搜索、滚轮浏览和单击指定结构；服务端只在所选结构类型中搜索 100 区块并保存结构与坐标。普通右键只消耗一点耐久并发射不可回收的视觉指示实体，黄金之眼本体及其绑定数据始终保留在手中；支持副手末影珍珠及旧版工作台配方修复耐久。光标作用距离扩展到 1024 格并保存目标维度：Shift 右键绑定方块面，普通右键激活已绑定目标；打开的远程菜单使用精确到玩家、容器和目标方块的会话校验，因此不会在超过原版 8 格后立即关闭。成功操作按距离收取经验，跨维度另收固定附加经验，失败不扣除；目标区块未加载时只在动作栏提示，不会强制加载区块。
- 魔法铅笔和 16 色魔法蜡笔恢复为虚构方块物品：具有方块/顶端薄板/半高悬空薄板/双层薄面楼梯及四种反转模式、次数与分数消耗、RGB 保存、对应眼镜的逐玩家显示/选取/碰撞规则和物品色彩渲染；彩色眼镜会继承蜡笔 RGB 并只消耗一次使用量。恢复同色次数合并与异色 RGB 混合的旧版特殊配方；已放置虚构方块现在只有手持魔法铅笔或魔法蜡笔时才能右键切换形态，空手和其他物品均不会修改。
- 开放式方块指南会从注册表动态收集全部 40 个方块物品、43 个独立物品和液态经验桶，共生成 84 个物品介绍条目；目录按机器、世界方块、可佩戴装备、工具、组件材料和流体六类编排，连同分类页共 91 页。全部 84 项均有对应现有功能、操作方式、容量或合成用途的中英文具体介绍，不再以分类通用句代替。
- 奇迹磁铁、史莱姆分析仪、计步器、制图助手、说明书、海绵棒、史诗橡皮擦、扳手和 `/dev/null` 的独立玩法。扳手会围绕实际点击面的有向轴旋转 `facing`、`axis`、`orientation` 和 16 段旋转属性；点击负方向面时反向旋转，按钮、拉杆和箱子要求潜行使用以避免抢占原版交互。
- 爆炸、背水一战、恶作剧三种数据驱动附魔及服务端效果。
- 转换旧版普通合成表并补充现代扁平化配方；当前模组资源目录含 91 个配方 JSON。
- JEI `19.25.0.323` 和 Jade `15.10.0+neoforge` 仅作为开发客户端的 `runtimeOnly` 测试模组，不是编译 API、打包内容或玩家必装依赖。

## 本轮问题修复说明

- 经验桶能从储液罐取出却不能倒入的原因：自定义 `XpBucketItem` 覆盖了 `BucketItem#useOn`，因此通用 `FluidUtil` 交互没有可靠进入“排空容器”分支。储液罐现在先显式识别经验桶、模拟确认可接收 1000 mB 后执行灌入并返还空桶，再回退到通用流体容器处理。
- 箭靶、自动铁砧、调漆机和村庄边界显示器让相邻方块透明的原因：它们使用非完整几何模型，但方块属性仍允许完整面遮挡。现已设置 `noOcclusion()`，相邻实体方块不再错误剔面。
- 箭靶增加独立 `DEPLOYED` 状态：红石控制立起/收起，箭命中只负责 10 tick 输出强度；状态切换时清理或掉落已插入箭并播放开合声。
- 旋转电梯贴图只有顶面使用方向纹路，侧面与底面使用普通电梯纹理；方块破坏器背面恢复孔洞，四周纹路按正面连续环绕。
- 经验花洒最初依赖计划方块 tick，测试中会出现不稳定停摆；现改为服务端方块实体 ticker，每 3 tick 拉取并喷出，只有经验球成功加入世界后才扣除流体。
- 捕兽夹旧模型原本依赖旧 TESR 的平移，直接烘焙后整体高出 7.5 像素；模型元素现统一下移 7.5 像素。
- 经验排水口未能抽取部分真实玩家经验的原因：`Player.totalExperience` 在通过“增加等级”等路径获得经验时可能仍为 0 或与可见等级不一致。现根据 `experienceLevel`、`experienceProgress` 和原版逐级经验公式计算当前可用经验，再使用原版经验变更方法扣除；测试专门覆盖了“可见等级为 5、`totalExperience` 为 0”的情况。
- 滑翔翼仍有抽搐的原因：即使朝向改为身体 yaw，滑翔翼实体和玩家实体仍由客户端分别进行位置/旋转网络插值，两条时间线的细小差值会直接表现成模型抖动。渲染器现在以玩家当前 partial tick 的插值位置和 `yBodyRotO/yBodyRot` 为唯一视觉锚点，滑翔翼自身网络位置只用于实体同步和剔除。
- 行李箱此前继承普通 `Entity`，每 tick 手工修改速度和坐标，既没有地面路径导航，也没有生物移动控制与正常插值。现改为 `PathfinderMob`，补充移动/跟随属性并使用地面导航追随主人；客户端更新间隔缩短为 1 tick，正常距离内完全取消传送式跟随。
- 方块放置器曾把侧面纹理同时当作背面和上下纹理，导致图案方向断裂；现改为显式六面模型，正面、带孔背面以及四个按朝向旋转的侧面分别映射。
- 喷洒器“水量”文字先前与容量柱、随后又与 3×3 槽位重叠；现固定在液量柱上方的标题行，容量柱本身提供当前值/容量悬停提示。
- 经验桶不应依靠潜行来绕过机器菜单。现在方块交互先尝试一次完整的 1000 mB 经验液转移，成功即消耗经验桶并返还空桶，无法转移时才走原版桶的世界放置或机器菜单逻辑。
- 风扇旧版扇叶子模型依赖 `+2.75` 模型像素的 Y 平移；现代合并模型此前漏掉了这一步。当前方块模型和资源生成脚本都固定补上该平移，扇叶中心与外框重新对齐。
- 物品投掷器与物品大炮的库存恢复为左侧连续 3×3 九宫格，并保留原有的速度/红石控制与目标状态/坐标侧栏；仅移除九宫格及侧栏外围多余的凹槽边框。
- 模板此前只有少量自定义图案且缺少贴面遮罩行为；现按旧版顺序恢复全部 15 个掩码，绘图台、物品渲染、画布覆盖层和实际透孔涂色共用同一份数据，避免“显示图案与实际效果不一致”。
- 画布上下面的单像素目标看似镜像，根因是工具把上面的 Z 坐标和下面的 Z 坐标按相反方向换算，实际渲染面无需翻转。现按 `CanvasRenderer` 的像素行方向修正，并将单像素行为放到新增的小油漆刷/小刮刀；主油漆刷和主刮刀恢复整面行为。另一个模板面无法刷漆的问题来自画布方块的默认模板交互先旋转模板并吞掉物品调用；现在刷子、刮刀和模板物品会跳过默认交互并正确进入各自的 `useOn`。
- 洒水器的移动横杆原本保留了旧 TESR 的高位坐标，合并为烘焙模型后显得过高；现下移 4 个模型像素。模型补上 `particle` 纹理，破坏粒子不再回退为紫黑缺失纹理。对照旧版 `Material.WATER` 与 `BlockLiquid.LEVEL` 后，先前直接返回原版 `WATER` 会产生可见水面，并在破坏时由水浸方块移除逻辑留下真实水源。现改为专用虚拟水体：复用原版水类型和水标签，但流速恒为零、不可替换/放置、旧方块状态为空气，客户端跳过其液体网格；同时保留旧版薄碰撞体。它只让机制把洒水器视作水，不会生成或残留流水。
- 绘图台输入原先只接受未加工模板，导致已带图案的模板不能再次加工；现在两种模板均可输入，输出仍按当前选择重建图案。翻页按钮和命中区域同步改为横向 `<`、`>`。
- 自动附魔台旧界面的档位按钮已完全移除。等级上限不再写死为 30：默认值来自原版第三档成本算法，运行时根据实际附魔强度和 NeoForge 等级事件更新；液态经验换算使用饱和运算，避免高上限模组触发整数溢出。
- 自绘机器界面此前使用直角矩形外框；现按 Minecraft 原版 `generic_54` 容器纹理的像素轮廓绘制四角，主面板和独立输入输出配置面板均为原版三像素切角圆角。
- 起重机背包和眼镜此前只是普通物品，右键仅写入一个开关，无法与原版装备栏、死亡掉落或其他装备逻辑兼容。它们现分别继承原版胸甲/头盔装备，能力读取装备槽，客户端用独立盔甲模型显示起重机吊臂、吊索、磁铁和眼镜。
- 道路此前仍是完整立方体模型；现恢复薄层、无碰撞、下方支撑和透光行为。投影仪此前只在方块周围生成三维粒子，没有实际显示高度图；现由方块实体渲染器按地图 9×9 高度值生成连续立体投影。天空方块此前只有普通静态模型；现恢复红石延迟、反转及激活时天空伪装/碰撞分离。
- 黄金之眼此前只是保存坐标后直接传送玩家，与旧版结构定位和眼球实体完全不同；现改为扫描指定结构并发射不可回收的视觉指示实体，同时让原物品留在手中并消耗耐久。光标此前只能同维度短距离操作；现保存维度并按实际距离和跨维度附加值扣除经验，但只操作当前已加载的目标区块。
- 魔法铅笔/蜡笔曾被误当成绘图台的普通铅笔而缺失。现恢复旧版虚构方块的颜色、形状、反转、次数与两种动态配方；小油漆刷/小刮刀仍是画布像素工具，二者用途明确分离。
- 行李箱此前虽然能手动物品化回收，但仍会进入普通实体死亡流程并丢失库存。现从伤害入口直接拒绝伤害，并在意外死亡/强制移除路径生成完整打包物品；掉落实体同时防伤害、防自然消失。
- 魔法铅笔/蜡笔初版把旧版 0.1 格悬空面误实现成实体半砖和普通楼梯，且用粒子代替眼镜可见性；现直接对照 `BlockImaginary`、`TileEntityImaginary` 和 `ItemImaginationGlasses`，恢复薄面几何以及铅笔、同色蜡笔、全彩和管理员眼镜各自的显示/选取/碰撞矩阵。彩色眼镜配方改为动态配方，正确继承 RGB 并返还少一次使用量的蜡笔。
- 起重机吊起目标后，如果玩家卸下背包或跨维度，目标曾可能保留 `noGravity`。背包现在同时保存目标维度，装备变更和背包 tick 均会释放目标并恢复重力。
- 天空方块初版只能在世界中右键反相，遗漏旧版普通/反相物品子类型和红石火把转换配方；现放置、破坏、创造物品栏和配方均保留反相状态。道路失去支撑时也改回旧版 `destroyBlock(..., true)` 语义，确保掉落自身。
- 光标远程界面曾只在打开瞬间可见，是因为机器和原版容器仍用玩家与方块不超过 8 格的 `stillValid` 校验。现在仅对本次光标打开的精确容器建立远程会话，并同时覆盖本模组机器菜单与原版方块实体容器；目标消失、方块变更、区块卸载或界面关闭时会立即撤销会话。未加载目标会提示“绑定方块所在区块当前未加载”，不会为远程操作制造区块加载票。
- 起重机吸住方块后的瞬移感来自原版 `FallingBlockEntity` 默认较慢的网络更新间隔。当前使用专用搬运实体逐 tick 同步，并以每 tick 最大移动量和渲染 partial tick 插值跟随磁头；大距离失联时才会纠正位置。起重机模型缺少长吊臂的原因是它是独立模型根节点，原版 `HumanoidModel` 只渲染六个身体部件；现已显式渲染该节点。
- 建筑引导器恢复六个独立半轴、11 种形状、12 种 `FrontAndTop` 三维朝向、旧版各面的按钮命中和红石启用投影；侧面按钮可增减或复制半轴，顶/底面按钮可切换形状和旋转，染料可改色。增强建筑引导器通电后可按投影顺序放置手持方块，每次放置一个；创造模式在引导器上方放置黑曜石时可填充或替换完整投影，并保留中心 3×3×3 安全区。

## 原模组差异审计

- 原版 40 个方块中，除按需求删除的脚手架外，其余 39 个均已注册并有对应行为；本项目另外保留用于现代扁平化内容的入口。旧版独立物品入口也均已注册，旧 metadata 材料拆成现代 ID。
- 旧版实现 `IConfigurableGuiSlots` 的机器只有自动铁砧、自动附魔台和经验装瓶机；三者的分通道面限制和主动拉取/推送已恢复。真空漏斗继续使用其原有的物品/经验输出面配置。
- 当前仍未做到逐算法一致的非联动细节：
  - 滑翔翼的热气流 Perlin 升力和客户端声学变率计按键/合成音尚未移植；基础滑翔、稳定渲染和耐久已实现。
  - 投影仪的高度数据、旋转与立体几何已恢复，但旧版旋转机械动画、投影锥光效没有逐像素复刻。
  - 天空方块使用当前天空颜色和透明状态模拟天空伪装；旧版依赖固定管线 framebuffer/stencil 的真正“天空洞”遮罩无法直接沿用到 1.21.1 渲染管线。
  - 道路的尺寸和玩法一致，但旧版依靠随机路石模型产生的细碎表面视觉暂以单一薄层模型代替。
- 上述视觉近似及滑翔翼高级细节不能因编译或烟雾测试通过而标为旧版逐像素/逐算法一致。ComputerCraft/OpenPeripheral 仍按需求排除。

## 数据与兼容性

- 方块实体的库存、液体、颜色/像素、目标位置和实体类型使用 1.21.1 NBT/数据组件保存。
- 储罐、画布、雕像、旗帜、油漆罐和电梯被破坏后会保留其必要数据。
- 旧世界中的 `openblocks:*` 注册名不能直接被 `openblocks_reborn:*` 自动替换；如需迁移旧存档，仍应先备份并使用专门的数据修复映射。
- OpenModsLib 已完全移除，当前实现仅使用 Minecraft 与 NeoForge API。
- 按当前需求，ComputerCraft 和 OpenPeripheral 联动明确不在本轮移植范围内。
- 删除脚手架后，曾经加载过旧注册项的开发存档会报告 `openblocks_reborn:scaffolding` 未知注册键并把它替换为空气；这是旧存档残留，不代表当前资源缺失。正式存档仍应先备份。

## 成果验收表

| 功能范围 | 实现状态 | 当前验证依据 | 验收 |
| --- | --- | --- | --- |
| 15 类机器 GUI 可打开及基本同步 | 已实现 | 23 项 GameTest 中覆盖主要机器；客户端烟雾测试逐个打开并按实际菜单语义截图 | ✅ |
| GUI 原版圆角、字体、槽位、按钮悬停及滑条拖动 | 已实现 | 2026-07-25 客户端截图核对；自绘主面板与独立配置面板使用原版三像素切角圆角，普通文字无阴影，按钮/滑条具 highlighted 状态；物品投掷器与物品大炮为无额外凹槽外框的连续 3×3 九宫格，并保留原有侧栏；喷洒器水量标签与槽位分离并有容量提示 | ✅ |
| 自动附魔单滑条与动态上限 | 已实现 | 不存在档位按钮；原版算法推导基线，NeoForge 等级事件提供模组上限；GameTest 验证 15 个有效书架时上限和所选等级均为原版基线 | ✅ |
| 主刷/主刮整面、小刷/小刮单像素与 15 种模板 | 已实现 | 原整面和模板行为已有 GameTest/客户端依据；2026-07-26 新增单像素工具、上下表面命中、模板面工具交互优先级、已加工模板重加工和左右翻页位置修正后仅按要求完成编译 | 🟡 |
| 自动铁砧/附魔台/装瓶机六面配置与主动传输 | 已实现 | GameTest 覆盖面能力拒绝/接受、主动拉取工具，以及装瓶机分面拉取空瓶/经验液、推送产物 | ✅ |
| 特殊方块及物品栏三维模型 | 已实现 | 客户端四方向画廊、物品展示墙截图；最新日志无缺失模型/纹理信息 | ✅ |
| 捕兽夹贴地与开合 | 已实现 | 模型包围盒最低点为 0；客户端模型画廊和捕获逻辑 GameTest 通过 | ✅ |
| 非完整模型相邻方块不透明 | 已实现 | 目标/自动铁砧/调漆机/村庄显示器均禁用完整面遮挡；客户端画廊无错误剔面 | ✅ |
| 翡翠活板门 | 已实现 | 用户游戏内反馈放置与功能成功；GameTest 覆盖 | ✅ |
| 绳梯贴面和默认方向 | 已实现 | GameTest 覆盖基础放置；最终手动朝向仍以用户下一次反馈为准 | 🟡 |
| 六向放置器、投掷器、破坏器及破坏器背面输出 | 已实现 | 编译和现有放置朝向/投掷 GameTest 覆盖；上下方向和背面容器仍需针对性游戏内复验 | 🟡 |
| 大炮模型、碰撞及 GUI | 已实现 | 用户确认模型；GameTest 覆盖指示器目标和发射，客户端打开 GUI | ✅ |
| 旋转电梯 | 已实现 | GameTest 使用服务端玩家验证抵达 EAST 目标后 yaw 为 90°；顶面方向纹路模型通过客户端加载 | ✅ |
| 储罐桶交互、连接边框和满宽液面 | 已实现 | GameTest 验证经验桶灌入与储罐网络；客户端连接模型截图通过 | ✅ |
| 经验排水口/花洒/装瓶机 | 已实现 | GameTest 验证可见等级存在但 `totalExperience=0` 时仍可直接抽取玩家经验、花洒背面拉液并喷出、40 tick 装瓶及自动输入输出 | ✅ |
| 洒水器横杆、破坏粒子与完整水体作用 | 已实现 | 横杆下移、模型 `particle` 纹理、不可见/不扩散的虚拟水体、原版水类型/水标签及旧版薄碰撞体均已写入代码/资源；2026-07-26 按要求仅完成编译，船/末影人及无残留水行为待游戏内复验 | 🟡 |
| 红石箭靶立起与 10 tick 命中输出 | 已实现 | GameTest 验证红石 `DEPLOYED` 状态；状态/模型 JSON 解析和客户端加载通过 | ✅ |
| 方块破坏器背孔与环绕侧纹 | 已实现 | 专用六面模型与资源生成器护栏；客户端模型加载无缺失 | ✅ |
| 方块放置器独立背面与环绕侧纹 | 已实现 | 专用六面模型与资源生成器护栏；客户端模型加载无缺失 | ✅ |
| 风扇扇叶与外框对齐 | 已实现 | 旧版子模型 `+2.75` 像素平移已同时写入模型和资源生成器；客户端资源加载无模型异常 | ✅ |
| 道路薄层、支撑与无碰撞 | 已实现 | GameTest 验证 1.6 像素外形、无碰撞和失去支撑后移除；资源和客户端模型加载通过 | ✅ |
| 投影仪地图、旋转与地形投影 | 已实现（视觉现代近似） | GameTest 验证空地图升级、高度图有效状态；模型烟雾客户端实际加载 9×9 立体投影渲染器且日志无异常 | ✅ |
| 天空方块红石/反转/伪装 | 已实现（视觉现代近似） | 红石延迟与反转状态写入方块实体；客户端天空色渲染器加载无异常，旧版 stencil 天空洞效果仍按差异审计记录 | 🟡 |
| 佩戴式眼镜与起重机 | 已实现 | GameTest 验证眼镜为 HEAD、起重机背包为 CHEST，并验证潜行下放/普通回收；模型烟雾测试从正面和侧面加载旧版尺寸背包、支架、吊臂与控制器全部状态模型，日志无模型/纹理异常 | ✅ |
| 黄金之眼定位、投射与修复 | 已实现 | GameTest 验证末影珍珠修复、目标数据保留、右键后本体仍为 1 个、耐久 +1 且指示实体不可回收；客户端实际打开含 34 个结构 ID 的搜索界面并截图。指定结构后的世界生成搜索结果仍接受用户实机复验 | 🟡 |
| 光标绑定、远程/跨维度菜单与经验消耗 | 已实现 | GameTest 验证普通右键不会重绑而会激活 14 格外大炮、远程菜单保持有效、Shift 右键重新绑定，以及 512 格外未加载区块不被强制加载、不打开菜单也不扣经验；实际客户端在 20 格外打开大炮 GUI 并稳定截图；最大距离为 1024 格 | ✅ |
| 扳手有向轴旋转 | 已实现 | GameTest 验证方块放置器围绕点击的顶面旋转；实现覆盖 Direction、Axis、FrontAndTop 与 16 段旋转属性，负方向面反向旋转 | ✅ |
| 建筑引导器与增强建筑引导器 | 已实现 | GameTest 验证 11 种投影、通电后按顺序放置一个方块及 3×3×3 安全区；12 种三维朝向、六半轴和旧版按钮命中逻辑由代码、方块状态资源及客户端资源加载共同验证 | ✅ |
| 魔法铅笔、蜡笔及虚构方块 | 已实现 | GameTest 验证 16 色/RGB、次数消耗、同色合并、异色混合、0.1 格旧版薄面包围盒、同色眼镜碰撞、彩色眼镜继承 RGB 及剩余次数；同时验证空手和普通物品不能切换形态、魔法铅笔可以切换形态。逐玩家渲染观感仍接受用户实机反馈 | 🟡 |
| 开放式方块指南全物品分类与具体介绍 | 已实现 | GameTest 按注册物品数断言生成 84 个独立条目、6 个分类页和 1 个欢迎页，共 91 页且未超过原版书本 100 页限制；生成器静态校验 84 个注册 ID 均有中英文具体介绍，原版书与帕莉秋条目共用这些翻译键 | ✅ |
| 行李箱三维实体、跟随、拾取、无敌和死亡保全 | 已实现 | GameTest 覆盖拾取/持久化、非瞬移寻路、拒绝伤害及强制死亡后原地打包物品仍含钻石；客户端画廊确认上移后的实体及底部脚部正常渲染 | ✅ |
| 悬挂式滑翔翼基础功能与稳定朝向 | 已实现 | 客户端实体模型加载无异常；渲染位置和身体 yaw 统一使用主人同一 partial tick 插值；飞行手感仍接受用户实机反馈 | 🟡 |
| 滑翔翼热气流/声学变率计 | 未实现 | 旧版可选高级飞行功能，已在差异审计中保留 | ⬜ |
| 自动铁砧/附魔台原版算法 | 已实现 | GameTest 覆盖附魔书合并升阶、Prior Work Penalty、原版重命名成本、15 个有效书架、选定等级、附魔种子、青金石与普通书转换 | ✅ |
| 经验桶标签及原版式右键优先级 | 已实现 | GameTest 验证 `c:buckets`/`c:buckets/experience`/`c:experience` 标签，并验证非潜行右键优先向自动铁砧灌入 1000 mB | ✅ |
| 睡袋 | 已实现 | 编译通过；夜间、怪物阻止及不修改重生点仍需游戏内复验 | 🟡 |
| JEI/Jade 开发环境 | `runtimeOnly` | 2026-07-24 客户端日志确认 JEI 完成启动、Jade 收到服务端配置 | ✅ |
| 删除 OpenBlocks 脚手架 | 已完成 | 源码、资源、生成脚本无 `scaffolding` 引用；旧测试存档警告已单独记录 | ✅ |
| ComputerCraft/OpenPeripheral 联动 | 按需求暂不实现 | 不纳入当前验收 | ⬜ |

## 自动验证命令

```powershell
.\gradlew.bat runGameTestServer --no-daemon
.\gradlew.bat runClient -PopenblocksSmokeTest --no-daemon
.\gradlew.bat build --no-daemon
```

- 23 项 GameTest 覆盖翡翠活板门/绳梯、储罐网络/保留数据、真空拾取、行李箱拾取/持久化/非瞬移寻路/无敌/死亡掉落保全、机器处理、自动铁砧附魔书合并/重命名/Prior Work Penalty、自动附魔台有效书架/动态等级上限/种子/青金石成本、任意 RGB 调漆、绘图台选择/打印、颜料刷整面填充、15 种模板的孔洞遮罩/旋转/保存、魔法铅笔/蜡笔次数与动态配方/旧版薄面/同色眼镜/限定形态切换物品、天空方块反相配方、黄金之眼修复/留在手中/不可回收指示实体、起重机下放与回收方向和搬运实体平滑追随、指南全物品条目数、原生装备槽、道路支撑破坏、投影仪地图升级、光标跨维度激活与扣经验、Shift 绑定、远程菜单和未加载区块保护、扳手有向轴旋转、建筑引导器 11 种形状及增强引导器单块建造、液态经验、经验桶标签与非潜行灌入、玩家可见等级与 `totalExperience` 不一致时的经验排水、花洒 1 B 缓冲、40 tick 装瓶、机器分面能力与主动传输、画布像素、按钮、高度图、金蛋、雕像、自定义附魔、旗帜、捕兽夹、放置朝向、投掷速度、字符牌、大炮目标、红石箭靶及旋转电梯朝向。
- 客户端烟雾测试会自动进入实际单人世界，在加载遮罩结束且每个菜单稳定渲染 10 帧后，按实际菜单类型打开并截图检查 15 类 GUI；随后放置当前 40 个方块和额外状态样本，生成画廊、物品模型墙及连接模型截图后自动退出。

### 最近一次验收：2026-07-26

- `.\gradlew.bat compileJava --no-daemon --console=plain`：成功。
- `.\gradlew.bat build --no-daemon --console=plain`：成功，生成 `build/libs/openblocks_reborn-1.0.0-1.21.1.jar`。
- `.\gradlew.bat runGameTestServer --no-daemon --console=plain`：23/23 项必需测试通过，GameTest 服务端正常自行退出；新增测试验证光标 Shift 绑定、14 格外远程大炮菜单、未加载区块保护、扳手旋转、建筑引导器投影/建造和起重机搬运实体平滑追随。
- 资源解析：`src/main/resources` 下 359 个 JSON 全部可解析，其中 91 个为模组配方；新增 8 个 JSON 为起重机控制器的升降/磁铁组合状态模型。
- `.\gradlew.bat runClient -PopenblocksSmokeTest --no-daemon --console=plain`：实际客户端完成脚本并自行退出，共生成 21 张本轮截图；15 张 GUI 截图名称与实际菜单内容一致。已人工核对自动附魔单滑条、绘图台镂空预览，以及物品投掷器/物品大炮无额外凹槽框的连续 3×3 九宫格与右侧控制/目标信息。
- `.\gradlew.bat runClient -PopenblocksModelSmokeTest --no-daemon --console=plain`：模型近景脚本让盔甲架实际穿戴魔法铅笔眼镜和起重机背包，生成 `openblocks_models_close.png`、`openblocks_crane_side.png` 等截图后自行退出；已核对完整 42 像素吊臂。
- `.\gradlew.bat runClient -PopenblocksCursorSmokeTest --no-daemon --console=plain`：实际客户端在距大炮 20 格处通过光标打开机器界面，等待 40 tick 后仍保持打开并生成 `openblocks_cursor_remote_gui.png`，随后正常自行退出。
- 最新模型客户端日志中缺失纹理、缺失模型、模型加载异常、崩溃和方块实体异常均为 0。
- JEI 和 Jade 均成功加载。测试存档仅出现已删除脚手架的旧注册键恢复警告，见“数据与兼容性”。

## 资源再生成

从相邻旧项目重新同步资源时执行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tools\generate_resources.ps1
```

## 2026-07-26 本轮最终增量复验

- 本节取代上方同日验收记录中的旧截图数量和旧增量断言说明。
- `runGameTestServer`：23/23 项必需测试通过；本轮进一步覆盖光标 Shift 绑定、远程菜单、未加载区块保护、扳手旋转、建筑引导器与增强建筑引导器，以及起重机搬运实体平滑追随。
- `runClient -PopenblocksSmokeTest`：实际打开 15 张机器 GUI 和黄金之眼 GUI，并生成 6 张画廊截图，共 22 张；黄金之眼界面正常列出当前世界的 34 个结构 ID。
- `runClient -PopenblocksModelSmokeTest`：正面与侧面近景均成功，起重机背包、支架、显式渲染的 42 像素吊臂、控制器状态模型和磁铁模型无缺失纹理/模型异常。
- `runClient -PopenblocksCursorSmokeTest`：在 20 格外通过光标打开大炮界面，40 tick 后仍保持打开并截图，验证远程 GUI 不再一闪关闭。
- `build`：成功；359 个资源 JSON 全部可解析，正式产物为 `build/libs/openblocks_reborn-1.0.0-1.21.1.jar`。

## 2026-07-26 帕莉秋手册可选集成

- 在模组元数据中加入 `patchouli` 可选前置，最低兼容 1.21.1-92；它不会被打入 OpenBlocks Reborn 的发布 JAR，也不会阻止未安装帕莉秋手册的环境启动。
- `openblocks_reborn:info_book` 保持原有稳定物品 ID 和配方。加载帕莉秋手册时，该物品改为打开 `openblocks_reborn:openblocks_guide`；未加载时继续打开原版成书格式的分类指南，因此旧存档和不使用帕莉秋手册的整合包无需迁移。
- 帕莉秋版本包含机器、世界方块、可佩戴装备、工具与设备、组件与材料、流体六个分类，并为当前全部 84 种注册物品生成独立条目；存在普通配方的条目同时带有配方页。
- `tools/guide_descriptions.json` 为 84 项中英文具体介绍的权威数据源；原版成书和帕莉秋条目均引用对应的 `message.openblocks_reborn.book.detail.<id>` 翻译键。`tools/generate_patchouli_book.py` 会校验说明 ID 与当前注册表完全一致，后续新增或删除方块/物品却未同步说明时会直接失败。
- 未安装帕莉秋时，首次右键会先用 `-2` 直接物品栏同步包把刚写入 `WRITTEN_BOOK_CONTENT` 的指南物品发送给客户端，再发送打开书本包，修复新获得指南因客户端仍持有旧物品数据而必须右键两次的问题。
- 按本轮要求未启动游戏或执行客户端验收；`.\gradlew.bat compileJava --no-daemon --console=plain` 编译成功。
- 静态完整性检查通过：`src/main/resources` 下 450 个 JSON 均可解析；注册表的 84 个物品 ID 与 `tools/guide_descriptions.json` 完全一致，中文、英文翻译各有 84 个具体说明键，帕莉秋也恰有 84 个独立条目且全部引用各自说明。
- Gradle 的 `client2` 运行配置已命名为 `Client 2 (Patchouli)`。最初使用的 `client2AdditionalRuntimeClasspath` 只会把 Patchouli 放入 FML 的旧式类库路径，无法作为模组发现；现改为在启动前由 `stageClient2Patchouli` 建立私有 Maven 根目录，并通过 `--fml.mods`/`--fml.mavenRoots` 让 FML 仅在 `client2` 中显式加载完整 Patchouli。普通 `client` 与 `client1` 不受影响。

## 2026-07-26 光标、起重机、扳手和建筑引导器复验

- 光标改为 Shift 右键绑定、普通右键激活。目标区块未加载时动作栏会明确提示，不会强制加载、打开菜单或扣除经验；已打开的远程菜单若随后卸载目标区块，也会提示并安全关闭。
- 23/23 项 GameTest 通过，覆盖远程大炮菜单保持有效、未加载区块保护、扳手有向轴旋转、建筑引导器 11 种形状、增强建筑引导器逐块建造和起重机搬运实体平滑追随。
- `runClient -PopenblocksCursorSmokeTest` 在 20 格外打开大炮界面并等待 40 tick，界面保持打开且生成 `run/screenshots/openblocks_cursor_remote_gui.png`；`runClient -PopenblocksModelSmokeTest` 验证显式渲染的完整 42 像素起重机吊臂。
- 重新生成 84 个帕莉秋物品条目后，`src/main/resources` 下 450 个 JSON 全部解析成功；`.\gradlew.bat build --no-daemon --console=plain` 构建成功。

## 2026-07-27 建筑引导器崩溃与起重机输入/渲染复验

- 修复通电建筑引导器的投影渲染崩溃：半透明填充与线框原先同时借用了 `BufferSource` 的共享构建器，切换到线框渲染类型会提前结束填充批次；现分别完整提交两个批次。
- 建筑引导器默认放置状态固定为 `top=UP`，因此没有减号的 `guide_top_new` 只出现在上下两面；其 12 种扳手朝向仍保留。资源生成脚本也会重建相同的 12 向方块状态，不再把它覆盖成无朝向模型。
- 起重机恢复旧版第一人称独立吊臂、黄黑钢索和 6/4/2 像素三层磁头。被吸附方块不再采用普通非生物实体收到位置包后的直接跳转，而是保存服务端目标并按生物实体使用的三客户端 tick 路径逐帧插值。
- 手持起重机控制器时，左键只切换磁头，不会攻击实体或挖掘方块；右键只控制钢索长度，不会打开、按动或以其他方式操作所指向的方块/实体。物品前置钩子和高优先级玩家交互事件共同拦截右键目标。
- `runGameTestServer` 的 23/23 项必需测试通过，新增断言覆盖引导器默认顶面、控制器禁挖掘/禁攻击/禁方块右键；`runClient -PopenblocksGuideSmokeTest` 在实际单人世界放置通电引导器并生成投影截图后正常自行退出，未再出现 `BufferBuilder.ensureBuilding` 崩溃。
- `runClient -PopenblocksModelSmokeTest` 切换第三/第一人称并生成 `openblocks_crane_side.png`、`openblocks_crane_first_person.png` 后正常自行退出；第一人称截图确认 42 像素吊臂从头顶沿准心方向延伸，旧版全亮黄黑警示纹、末端钢索和磁头均正常。
- 最终 `build` 成功，`src/main/resources` 下 450 个 JSON 全部可解析；发布产物为 `build/libs/openblocks_reborn-1.0.0-1.21.1.jar`。

## 2026-07-27 起重机磁头方向与第三人称坐标修正

- 磁头不再用负 Y 缩放翻转模型；现在以钢索端点作为 2×2 窄顶的顶面，向下依次扩展为 4×4 中层和 6×6 宽底，恢复旧版“下宽上窄”的三层轮廓。
- 玩家第三人称的长吊臂、钢索和磁头统一移到 `AFTER_ENTITIES` 世界渲染阶段：吊臂从身体朝向后移 0.45 格的背包支点出发，沿头部偏航朝前；钢索和磁头直接使用相机相对世界坐标，因此不会再被 `RenderPlayerEvent` 的玩家模型矩阵二次旋转。
- 背包主体与 24 像素竖支架仍由原版盔甲层随身体动画渲染；盔甲架等非玩家展示实体继续使用旧版自定义吊臂子节点，避免展示模型回退。
- `runClient -PopenblocksModelSmokeTest` 在实际单人世界完成第三/第一人称截图并正常自行退出；第三人称截图确认 42 像素吊臂从玩家背包上方朝视线前方延伸，不再朝玩家背后/镜头突出。最终 `build` 成功。
